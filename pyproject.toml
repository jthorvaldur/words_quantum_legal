[project]
name = "words-quantum-legal"
version = "0.1.0"
description = "Quantum word parse — structural language analysis CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
qwparse = "cli.qwparse:main"
